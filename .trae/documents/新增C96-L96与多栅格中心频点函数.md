# 在 gnpy/core/utils.py 新增 C96/L96 及多栅格中心频点函数

## Summary

在 [gnpy/core/utils.py](file:///c:/g00572545/codes/github/oopt-gnpy/gnpy/core/utils.py) 中新增：

1. 常量 `DWDM_BAND_RANGES`：C96 / L96 波段占用带宽边缘。
2. `itu_grid_center_frequencies(f_min, f_max, spacing, anchor_frequency=193.1e12)`：通用函数，按任意 GRID 间隔（50/75/100/150/200/250/300 GHz …）返回**完整落在波段占用范围内**的信道中心频点，栅格锚定 193.1 THz（ITU-T G.694.1）。
3. `band_center_frequencies(band_name, spacing=50e9)`：按波段名（`C96` / `L96`）获取中心频点，内部调用上面通用函数。

两个函数都带 doctest（`pytest.ini` 的 `addopts = --doctest-modules` 会执行）。

## Current State Analysis

- [utils.py#L426-L453](file:///c:/g00572545/codes/github/oopt-gnpy/gnpy/core/utils.py#L426-L453) 已有 `automatic_nch(f_min, f_max, spacing)` 与 `automatic_fmax(f_min, spacing, nch)`，均把 `f_min/f_max` 当作**中心频点**看待（`automatic_nch` 返回 `int((f_max - f_min) // spacing) + 1`），且都带 `>>>` doctest。新增函数紧跟在 `automatic_fmax` 之后。
- 仓库中**没有**任何"按 spacing 生成中心频点数组"的可用函数；现有写法都是内联：[info.py#L440-L441](file:///c:/g00572545/codes/github/oopt-gnpy/gnpy/core/info.py#L440-L441) 的 `f_min + spacing * i`。
- 栅格锚点常量 `DEFAULT_GRID = 0.00625e12` 与 `frequency_to_n/nvalue_to_frequency`（锚定 193.1 THz）在 [spectrum_assignment.py#L32-L32](file:///c:/g00572545/codes/github/oopt-gnpy/gnpy/topology/spectrum_assignment.py#L32-L32)、[spectrum_assignment.py#L175-L202](file:///c:/g00572545/codes/github/oopt-gnpy/gnpy/topology/spectrum_assignment.py#L175-L202)，但那里只做单点 n↔f 换算，没有批量生成。
- [parameters.py#L705-L731](file:///c:/g00572545/codes/github/oopt-gnpy/gnpy/core/parameters.py#L705-L731) 有 `FrequencyBand` 与 `DEFAULT_BANDS_DEFINITION`（`CBAND` 191.3–196.0 THz、`LBAND` 187–189 THz）与 `find_band_name()`，语义是"多波段放大器按中心频率索引波段名"，与本次 C96/L96 占用带宽不同，**本次不改动它**。
- [parameters.py#L21](file:///c:/g00572545/codes/github/oopt-gnpy/gnpy/core/parameters.py#L21) 存在 `from gnpy.core.utils import convert_length`，因此 **`utils.py` 不能反向 import `parameters.py`**（循环导入）→ 波段常量必须定义在 `utils.py` 内。
- 风格约束：[tox.ini#L41-L44](file:///c:/g00572545/codes/github/oopt-gnpy/tox.ini#L41-L44) `max-line-length = 120`，`ignore = N806 W503 C901`；`utils.py` 用 `from numpy import <符号>` 扁平导入、单引号为主、无 `__all__`；[pytest.ini](file:///c:/g00572545/codes/github/oopt-gnpy/pytest.ini) 只有 `addopts = --doctest-modules`（无 `testpaths`），`tests/` 下**没有** `test_utils.py`。

用户已确认的三项约定：
- C96/L96 给定范围视为**占用带宽边缘**（96 × 50 GHz = 4.8 THz）。
- 非 50 GHz 栅格以 **193.1 THz 为锚点**。
- 信道数**由带宽自动决定**（只保留完整落在占用范围内的中心频点）。

## Proposed Changes

只改一个文件：[gnpy/core/utils.py](file:///c:/g00572545/codes/github/oopt-gnpy/gnpy/core/utils.py)。

### 1. 补充 numpy 导入（第 18 行）

现有导入行加上 `arange, ceil, floor` 会超过 120 列，改为括号换行形式：

```python
from numpy import (pi, cos, sqrt, log10, linspace, zeros, shape, where, logical_and, mean, array, isinf,
                   arange, ceil, floor)
```

### 2. 在 `automatic_fmax`（第 453 行）之后新增代码

```python
# DWDM 波段占用带宽边缘 [Hz]：C96 / L96 各为 96 x 50 GHz = 4.8 THz
DWDM_BAND_RANGES = {
    'C96': (191.275e12, 196.075e12),
    'L96': (186.275e12, 191.075e12),
}


def itu_grid_center_frequencies(f_min, f_max, spacing, anchor_frequency=193.1e12):
    """Center frequencies of the channels fitting in a band, on an ITU-T G.694.1 grid

    The grid is anchored at 193.1 THz: the nominal center frequencies are
    193.1 + n * spacing, with n a positive or negative integer including 0.
    Only the channels whose whole slot ([center - spacing / 2, center + spacing / 2])
    fits in [f_min, f_max] are returned.

    :param f_min Lowest frequency of the band (occupied spectrum lower edge) [Hz]
    :param f_max Highest frequency of the band (occupied spectrum upper edge) [Hz]
    :param spacing Grid/channel spacing [Hz]
    :param anchor_frequency Frequency the grid is anchored at [Hz]
    :return Sorted array of the channel center frequencies [Hz], empty if none fits

    >>> len(itu_grid_center_frequencies(191.275e12, 196.075e12, 50e9))
    96
    >>> round(itu_grid_center_frequencies(191.275e12, 196.075e12, 50e9)[0] * 1e-12, 4)
    191.3
    >>> round(itu_grid_center_frequencies(191.275e12, 196.075e12, 50e9)[-1] * 1e-12, 4)
    196.05
    >>> len(itu_grid_center_frequencies(191.275e12, 196.075e12, 75e9)), \
round(itu_grid_center_frequencies(191.275e12, 196.075e12, 75e9)[-1] * 1e-12, 4)
    (63, 196.025)
    >>> len(itu_grid_center_frequencies(191.275e12, 196.075e12, 1000e9))
    0
    """
    tolerance = 1e-9
    first_n = int(ceil((f_min + spacing / 2 - anchor_frequency) / spacing - tolerance))
    last_n = int(floor((f_max - spacing / 2 - anchor_frequency) / spacing + tolerance))
    if last_n < first_n:
        return array([])
    return anchor_frequency + arange(first_n, last_n + 1) * spacing


def band_center_frequencies(band_name, spacing=50e9):
    """Center frequencies of the channels of a predefined DWDM band

    :param band_name Band name, one of the keys of DWDM_BAND_RANGES ('C96', 'L96') [str]
    :param spacing Grid/channel spacing [Hz]
    :return Sorted array of the channel center frequencies [Hz]

    >>> len(band_center_frequencies('C96'))
    96
    >>> round(band_center_frequencies('C96')[0] * 1e-12, 4)
    191.3
    >>> round(band_center_frequencies('C96')[-1] * 1e-12, 4)
    196.05
    >>> round(band_center_frequencies('L96')[0] * 1e-12, 4), \
round(band_center_frequencies('L96')[-1] * 1e-12, 4)
    (186.3, 191.05)
    >>> len(band_center_frequencies('C96', 100e9))
    47
    """
    f_min, f_max = DWDM_BAND_RANGES[band_name.upper()]
    return itu_grid_center_frequencies(f_min, f_max, spacing)
```

说明：
- 用 `ceil/floor` 把 `n` 收进"完整落在带内"的整数区间；`tolerance = 1e-9`（n 的量级为 1e2，绝对容差足够）用于吸收 `(196.05e12 - 193.1e12) / 50e9` 这类浮点误差，避免少算一个信道。
- 返回 `numpy.ndarray`（与 `arrange_frequencies` 返回 ndarray 一致）；没有频点能完整落在带内时返回空数组，不做额外校验、不抛异常。
- doctest 里用 `len()` 和 `round(x * 1e-12, 4)` 断言，避免 ndarray 的 repr 噪声；多值断言用 `\` 续行（与 `find_common_range` 的写法一致），每行 ≤ 120 列。

## Assumptions & Decisions

1. 按用户确认：给定范围是**占用带宽边缘**；50 GHz 时 C96 中心为 191.3 … 196.05 THz（96 个）、L96 为 186.3 … 191.05 THz（96 个），与 193.1 THz 锚点的 50 GHz 栅格完全吻合。
2. 按用户确认：非 50 GHz 栅格锚定 193.1 THz，信道数由带宽自动决定。**更正**：提问时选项里写的 75/100/150/200/250/300 GHz → 64/48/32/24/19/16 是按"以带下边缘起算"手算的，按 193.1 锚点的实测值为 **63/47/31/23/18/15**（已本地验证）。各间距下的首末中心频点如下，作为实现完成后的对照基准：

   | spacing | C96 数量 | C96 首/末 (THz) | L96 数量 | L96 首/末 (THz) |
   |---|---|---|---|---|
   | 50 GHz | 96 | 191.3 / 196.05 | 96 | 186.3 / 191.05 |
   | 75 GHz | 63 | 191.375 / 196.025 | 63 | 186.35 / 191.0 |
   | 100 GHz | 47 | 191.4 / 196.0 | 47 | 186.4 / 191.0 |
   | 150 GHz | 31 | 191.45 / 195.95 | 32 | 186.35 / 191.0 |
   | 200 GHz | 23 | 191.5 / 195.9 | 23 | 186.5 / 190.9 |
   | 250 GHz | 18 | 191.6 / 195.85 | 18 | 186.6 / 190.85 |
   | 300 GHz | 15 | 191.6 / 195.8 | 15 | 186.5 / 190.7 |

3. 常量放 `utils.py` 而非 `parameters.py`：`parameters.py` 已 import `utils.py`，反向 import 会循环；且不改动 `DEFAULT_BANDS_DEFINITION` / `find_band_name`（语义为多波段放大器索引，CBAND 定义 191.3–196.0 THz 与本次 C96 占用范围 191.275–196.075 THz 不可混用）。
4. 不加类型注解，与紧邻的 `automatic_nch` / `automatic_fmax` 保持一致；单引号；行宽 ≤ 120。
5. 不新建 `tests/test_utils.py`：仓库对纯函数的既有验证手段就是 doctest，`--doctest-modules` 已覆盖 `gnpy/core/utils.py`。
6. 不改动 `create_input_spectral_information` / `create_arbitrary_spectral_information`；新函数的返回值可直接喂给 `create_arbitrary_spectral_information(frequency=...)`。

## Verification

1. doctest（等于新增单测）：
   `.\.venv\Scripts\python.exe -m pytest gnpy/core/utils.py -q` → 期望全部通过。
2. 静态检查：`flake8 gnpy/core/utils.py` → 无新增告警（CI 只对 `git diff` 的改动行做 flake8）。
3. 数值对照（一次性校验，不落盘）：
   `.\.venv\Scripts\python.exe -c "from gnpy.core.utils import band_center_frequencies as f; print([(s, len(f('C96', s))) for s in (50e9, 75e9, 100e9, 150e9, 200e9, 250e9, 300e9)])"` C96 期望 `[96, 63, 47, 31, 23, 18, 15]`，与上表一致。
4. 与既有函数自洽：
   `.\.venv\Scripts\python.exe -c "from gnpy.core.utils import band_center_frequencies as f; from gnpy.core.info import create_input_spectral_information as c; si = c(f_min=191.3e12, f_max=196.05e12, roll_off=0.15, baud_rate=32e9, spacing=50e9, tx_osnr=40.0, tx_power=1e-3); assert si.number_of_channels == 96; assert (si.frequency == f('C96')).all(); print('ok')"` → 打印 `ok`（说明 C96 的中心频点与现有 `create_input_spectral_information` 生成结果逐点一致）。
5. 回归：`.\.venv\Scripts\python.exe -m pytest -q`（或至少 `pytest gnpy/core -q`）确认导入改动未破坏既有 doctest。
