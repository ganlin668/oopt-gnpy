# 在 gnpy/bplab/utils.py 新增 C96/L96 及多栅格中心频点函数（纯新增，不改动 GNPy 原始代码）

## Summary

为避免与上游 oopt-gnpy 远程仓库产生冲突，本次**不修改 `gnpy/` 下任何原始文件**（不删、不改），只往已存在的自建目录 `gnpy/bplab/` 里新增内容：

1. [gnpy/bplab/utils.py](file:///c:/g00572545/codes/github/oopt-gnpy/gnpy/bplab/utils.py)（当前为空文件）：新增常量 `DWDM_BAND_RANGES`（C96 / L96 占用带宽边缘）+ 两个函数
   - `itu_grid_center_frequencies(f_min, f_max, spacing, anchor_frequency=193.1e12)`：通用，按任意 GRID 间隔（50/75/100/150/200/250/300 GHz …）返回完整落在占用范围内的信道中心频点，栅格锚定 193.1 THz（ITU-T G.694.1）。
   - `band_center_frequencies(band_name, spacing=50e9)`：按波段名（`C96` / `L96`）取中心频点，内部调用上面通用函数。
2. [gnpy/bplab/__init__.py](file:///c:/g00572545/codes/github/oopt-gnpy/gnpy/bplab/__init__.py)（当前为空文件）：补上仓库强制的文件头（见下方 Current State Analysis 第 3 点，不补则 CI 的红）。
3. **新建** `tests/bplab/test_utils.py`：用 pytest 覆盖波段常量、各 spacing 数量矩阵、栅格锚点、等间隔、带内/极大性、边界情况，以及与 `create_input_spectral_information` 的一致性。

`gnpy/bplab/utils.py` 里的函数同时带少量 doctest（仓库既有风格，`pytest.ini` 的 `addopts = --doctest-modules` 会自动执行）。

## Current State Analysis

1. `gnpy/bplab/` 已经存在（自建目录），其中 [__init__.py](file:///c:/g00572545/codes/github/oopt-gnpy/gnpy/bplab/__init__.py) 与 [utils.py](file:///c:/g00572545/codes/github/oopt-gnpy/gnpy/bplab/utils.py) **都是空文件**；`tests/bplab/` 尚不存在（`tests/` 下只有 `data/`、`invocation/`）。
2. 原 `gnpy/core/utils.py` 的方案已废弃：改为只在 `gnpy/bplab/utils.py` 新增，因此**不需要**改动 `gnpy/core/utils.py` 的 numpy 导入行，也不动 `gnpy/core/parameters.py` 的 `DEFAULT_BANDS_DEFINITION` / `find_band_name`（其语义是"多波段放大器按中心频率索引波段名"，CBAND 定义 191.3–196.0 THz，与本次 C96 占用范围 191.275–196.075 THz 不同，不可混用）。
3. **CI 强制文件头**：[tests/test_opensource_compliancy.py#L64-L114](file:///c:/g00572545/codes/github/oopt-gnpy/tests/test_opensource_compliancy.py#L64-L114) 的 `test_file_headers` 会 glob `gnpy/**/*.py` 与 `tests/**/*.py`，要求每个文件都含 `# -*- coding: utf-8 -*-`、`SPDX-License-Identifier: BSD-3-Clause`、`Copyright (C) ... Telecom Infra Project`、`see AUTHORS.rst` 四项。排除名单只有 `gnpy/__init__.py`、`gnpy/core/__init__.py`、`gnpy/tools/__init__.py`、`gnpy/topology/__init__.py`、`tests/__init__.py`，**`gnpy/bplab/__init__.py` 与 `gnpy/bplab/utils.py` 不在排除名单里**，两个空文件现在就会让该测试失败，必须补头。
4. 仓库风格约束：[tox.ini#L41-L44](file:///c:/g00572545/codes/github/oopt-gnpy/tox.ini#L41-L44) `max-line-length = 120`、`ignore = N806 W503 C901`；`gnpy/core/utils.py` 用 `from numpy import <符号>` 扁平导入、单引号为主、无 `__all__`、函数普遍带 `>>>` doctest（如 [automatic_nch](file:///c:/g00572545/codes/github/oopt-gnpy/gnpy/core/utils.py#L426-L439)）。
5. [pytest.ini](file:///c:/g00572545/codes/github/oopt-gnpy/pytest.ini) 只有 `addopts = --doctest-modules`，无 `testpaths`/`norecursedirs`；从仓库根运行，新增的 `gnpy/bplab/utils.py` doctest 与 `tests/bplab/test_utils.py` 都会被收集。`tests/` 有 `__init__.py`，子目录 `tests/data/` 没有，故 `tests/bplab/` 也不加 `__init__.py`（保持一致）。
6. 参考实现：仓库**没有**"按 spacing 生成中心频点数组"的现成函数，只有内联写法 [info.py#L440-L441](file:///c:/g00572545/codes/github/oopt-gnpy/gnpy/core/info.py#L440-L441)；栅格锚点 193.1 THz 的单点换算在 [spectrum_assignment.py#L175-L202](file:///c:/g00572545/codes/github/oopt-gnpy/gnpy/topology/spectrum_assignment.py#L175-L202)。返回的频点数组可直接喂给 [create_arbitrary_spectral_information](file:///c:/g00572545/codes/github/oopt-gnpy/gnpy/core/info.py#L374-L395)（首参 `frequency` 支持任意数组）。

用户已确认的三项语义约定：给定范围视为**占用带宽边缘**；非 50 GHz 栅格**锚定 193.1 THz**；信道数**由带宽自动决定**。

## Proposed Changes

### 文件 1（修改现有空文件）：gnpy/bplab/__init__.py

补上仓库标准文件头（内容仅此，不写代码）：

```python
# -*- coding: utf-8 -*-

# SPDX-License-Identifier: BSD-3-Clause
# gnpy.bplab: BPLab add-on package
# Copyright (C) 2025 Telecom Infra Project and GNPy contributors
# see AUTHORS.rst for a list of contributors
```

### 文件 2（修改现有空文件）：gnpy/bplab/utils.py

写入文件头 + 模块 docstring + 常量 + 两个函数：

```python
# -*- coding: utf-8 -*-

# SPDX-License-Identifier: BSD-3-Clause
# gnpy.bplab.utils: BPLab add-on frequency grid utilities
# Copyright (C) 2025 Telecom Infra Project and GNPy contributors
# see AUTHORS.rst for a list of contributors

"""
gnpy.bplab.utils
================

BPLab 自建的频点工具函数（纯新增，不修改 gnpy 原有代码）。
"""

from numpy import arange, array, ceil, floor

# DWDM 波段占用带宽边缘 [Hz]：C96 / L96 各为 96 x 50 GHz = 4.8 THz
DWDM_BAND_RANGES = {
    'C96': (191.275e12, 196.075e12),
    'L96': (186.275e12, 191.075e12),
}


def itu_grid_center_frequencies(f_min, f_max, spacing, anchor_frequency=193.1e12):
    """Center frequencies of the channels fitting in a band, on an ITU-T G.694.1 grid

    The grid is anchored at 193.1 THz: the nominal center frequencies are
    193.1 + n * spacing, with n a positive or negative integer including 0.
    Only the channels whose whole slot ([center - spacing / 2, center + spacing / 2])
    fits in [f_min, f_max] are returned.

    :param f_min Lowest frequency of the band (occupied spectrum lower edge) [Hz]
    :param f_max Highest frequency of the band (occupied spectrum upper edge) [Hz]
    :param spacing Grid/channel spacing [Hz]
    :param anchor_frequency Frequency the grid is anchored at [Hz]
    :return Sorted array of the channel center frequencies [Hz], empty if none fits

    >>> len(itu_grid_center_frequencies(191.275e12, 196.075e12, 50e9))
    96
    >>> round(itu_grid_center_frequencies(191.275e12, 196.075e12, 50e9)[0] * 1e-12, 4)
    191.3
    >>> round(itu_grid_center_frequencies(191.275e12, 196.075e12, 50e9)[-1] * 1e-12, 4)
    196.05
    >>> len(itu_grid_center_frequencies(191.275e12, 196.075e12, 75e9))
    63
    >>> len(itu_grid_center_frequencies(191.3e12, 191.4e12, 1e12))
    0
    """
    tolerance = 1e-9
    first_n = int(ceil((f_min + spacing / 2 - anchor_frequency) / spacing - tolerance))
    last_n = int(floor((f_max - spacing / 2 - anchor_frequency) / spacing + tolerance))
    if last_n < first_n:
        return array([])
    return anchor_frequency + arange(first_n, last_n + 1) * spacing


def band_center_frequencies(band_name, spacing=50e9):
    """Center frequencies of the channels of a predefined DWDM band

    :param band_name Band name, one of the keys of DWDM_BAND_RANGES ('C96', 'L96') [str]
    :param spacing Grid/channel spacing [Hz]
    :return Sorted array of the channel center frequencies [Hz]

    >>> len(band_center_frequencies('C96'))
    96
    >>> round(band_center_frequencies('C96')[0] * 1e-12, 4)
    191.3
    >>> round(band_center_frequencies('L96')[-1] * 1e-12, 4)
    191.05
    >>> len(band_center_frequencies('C96', 100e9))
    47
    """
    f_min, f_max = DWDM_BAND_RANGES[band_name.upper()]
    return itu_grid_center_frequencies(f_min, f_max, spacing)
```

说明：
- 用 `ceil/floor` 把 n 收进"完整落在带内"的整数区间（判定条件：中心频点 ∈ `[f_min + spacing/2, f_max − spacing/2]`）；`tolerance = 1e-9` 用于吸收 `(196.05e12 − 193.1e12) / 50e9` 这类浮点误差，n 的量级为 1e2，绝对容差足够。
- 返回 `numpy.ndarray`；没有频点能完整落在带内时返回空数组，不抛异常、不做额外校验。
- `band_name` 大小写不敏感（内部 `.upper()`）；未知波段名保持 Python 默认的 `KeyError`。
- 不加类型注解，与仓库 `gnpy/core/utils.py` 中 `automatic_nch` / `automatic_fmax` 风格一致；单引号；行宽 ≤ 120。

### 文件 3（新建）：tests/bplab/test_utils.py

```python
# -*- coding: utf-8 -*-

# SPDX-License-Identifier: BSD-3-Clause
# test_bplab_utils
# Copyright (C) 2025 Telecom Infra Project and GNPy contributors
# see AUTHORS.rst for a list of contributors

"""
Unit tests for the frequency grid helpers in gnpy.bplab.utils
"""
import pytest
from numpy import arange
from numpy.testing import assert_allclose

from gnpy.bplab.utils import DWDM_BAND_RANGES, band_center_frequencies, itu_grid_center_frequencies
from gnpy.core.info import create_input_spectral_information

SPACINGS = (50e9, 75e9, 100e9, 150e9, 200e9, 250e9, 300e9)
# 各 spacing 下完整落在占用范围内的信道数
EXPECTED_NCH = {
    'C96': (96, 63, 47, 31, 23, 18, 15),
    'L96': (96, 63, 47, 32, 23, 18, 15),
}
# 1.9e14 Hz 量级的双精度浮点误差约 0.05 Hz，取 1 Hz 作为比较容差
FREQ_EPS = 1.0


def test_dwdm_band_ranges():
    assert DWDM_BAND_RANGES['C96'] == (191.275e12, 196.075e12)
    assert DWDM_BAND_RANGES['L96'] == (186.275e12, 191.075e12)


@pytest.mark.parametrize('band_name', ('C96', 'L96'))
def test_channel_count_per_spacing(band_name):
    assert tuple(len(band_center_frequencies(band_name, s)) for s in SPACINGS) == EXPECTED_NCH[band_name]


def test_c96_on_50ghz_grid():
    centers = band_center_frequencies('C96')
    assert len(centers) == 96
    assert centers[0] == pytest.approx(191.3e12, abs=FREQ_EPS)
    assert centers[-1] == pytest.approx(196.05e12, abs=FREQ_EPS)
    assert_allclose(centers, 191.3e12 + 50e9 * arange(96), atol=FREQ_EPS)


def test_band_name_is_case_insensitive():
    assert_allclose(band_center_frequencies('c96'), band_center_frequencies('C96'))
    assert_allclose(band_center_frequencies('l96', 100e9), band_center_frequencies('L96', 100e9))


def test_unknown_band_name():
    with pytest.raises(KeyError):
        band_center_frequencies('C80')


@pytest.mark.parametrize('band_name', ('C96', 'L96'))
def test_grid_is_uniform_anchored_and_within_band(band_name):
    f_min, f_max = DWDM_BAND_RANGES[band_name]
    for spacing in SPACINGS:
        centers = band_center_frequencies(band_name, spacing)
        assert len(centers) > 0
        # 等间隔
        assert_allclose(centers[1:] - centers[:-1], spacing)
        # 全部落在 193.1 THz 锚点的栅格上
        n = (centers - 193.1e12) / spacing
        assert_allclose(n, n.round())
        # 每个信道的整个 slot 都在占用范围内
        assert (centers - spacing / 2 >= f_min - FREQ_EPS).all()
        assert (centers + spacing / 2 <= f_max + FREQ_EPS).all()
        # 极大性：栅格上下各再取一个点都会越界
        assert centers[0] - spacing < f_min + spacing / 2 + FREQ_EPS
        assert centers[-1] + spacing > f_max - spacing / 2 - FREQ_EPS


def test_no_channel_fits():
    assert len(itu_grid_center_frequencies(191.3e12, 191.4e12, 1e12)) == 0
    assert len(band_center_frequencies('C96', 5000e9)) == 0


def test_consistency_with_create_input_spectral_information():
    centers = band_center_frequencies('C96')
    si = create_input_spectral_information(f_min=centers[0], f_max=centers[-1], roll_off=0.15,
                                           baud_rate=32e9, spacing=50e9, tx_osnr=40.0, tx_power=1e-3)
    assert si.number_of_channels == 96
    assert_allclose(si.frequency, centers)
```

说明：
- 不加 `tests/bplab/__init__.py`，与 `tests/data/` 的既有约定一致。
- `test_consistency_with_create_input_spectral_information` 把新函数与既有 `create_input_spectral_information` 对齐（后者内部就是 `f_min + spacing * i`），保证新老写法结果逐点一致。
- `test_grid_is_uniform_anchored_and_within_band` 同时覆盖了 7 种 spacing、锚点、带内与"极大性"（再多一个信道就出界），是 ceil/floor 逻辑的核心回归。

## Assumptions & Decisions

1. 按用户要求：只在 `gnpy/bplab/` 新增，不改 `gnpy/core/utils.py`、`gnpy/core/parameters.py` 等原始文件；测试放 `tests/bplab/`。
2. `gnpy/bplab/__init__.py` 与 `gnpy/bplab/utils.py` 是**自建目录下的空文件**，为通过 `tests/test_opensource_compliancy.py::test_file_headers` 必须补文件头；这是补全而非改写逻辑（两者目前为空）。
3. 按用户确认：给定范围是**占用带宽边缘**；50 GHz 时 C96 中心 191.3 … 196.05 THz（96 个）、L96 中心 186.3 … 191.05 THz（96 个），正好落在 193.1 THz 锚点的 50 GHz 栅格上。
4. 按用户确认：非 50 GHz 栅格锚定 193.1 THz，信道数由带宽自动决定。**更正提问时选项中的估算**：那里的 64/48/32/24/19/16 是按"以带下边缘起算"手算的；按 193.1 锚点的实测值为 **63/47/31/23/18/15**（C96；L96 为 63/47/32/23/18/15，150 GHz 一项不同，已本地验证）。实现完成后的对照基准：

   | spacing | C96 数量 | C96 首/末 (THz) | L96 数量 | L96 首/末 (THz) |
   |---|---|---|---|---|
   | 50 GHz | 96 | 191.3 / 196.05 | 96 | 186.3 / 191.05 |
   | 75 GHz | 63 | 191.375 / 196.025 | 63 | 186.35 / 191.0 |
   | 100 GHz | 47 | 191.4 / 196.0 | 47 | 186.4 / 191.0 |
   | 150 GHz | 31 | 191.45 / 195.95 | 32 | 186.35 / 191.0 |
   | 200 GHz | 23 | 191.5 / 195.9 | 23 | 186.5 / 190.9 |
   | 250 GHz | 18 | 191.6 / 195.85 | 18 | 186.6 / 190.85 |
   | 300 GHz | 15 | 191.6 / 195.8 | 15 | 186.5 / 190.7 |

5. 常量定义在 `gnpy/bplab/utils.py` 内部，不 import `gnpy/core/parameters.py` 以避免任何耦合；`DEFAULT_BANDS_DEFINITION` / `find_band_name` 保持原样不动。
6. 模块内保留少量 doctest（仓库既有风格且会被 `--doctest-modules` 执行），完整参数化验证放在 `tests/bplab/test_utils.py`。
7. 不新增文档（`docs/`）也不改任何 CLI 入口。

## Verification

1. 新增单测：`.\.venv\Scripts\python.exe -m pytest tests/bplab/test_utils.py -v` → 期望全部通过。
2. doctest：`.\.venv\Scripts\python.exe -m pytest gnpy/bplab/utils.py -q` → 期望通过。
3. CI 强约束回归（两个空文件补头后才能过）：
   `.\.venv\Scripts\python.exe -m pytest tests/test_opensource_compliancy.py::test_file_headers -q` → 期望通过。
4. 静态检查：`flake8 gnpy/bplab/utils.py tests/bplab/test_utils.py` → 无告警（行宽 120）。
5. 数值对照（一次性检查，不落盘）：
   `.\.venv\Scripts\python.exe -c "from gnpy.bplab.utils import band_center_frequencies as f; print([len(f('C96', s)) for s in (50e9, 75e9, 100e9, 150e9, 200e9, 250e9, 300e9)])"` → 期望 `[96, 63, 47, 31, 23, 18, 15]`，与上表一致。
6. 回归：`.\.venv\Scripts\python.exe -m pytest -q`（至少 `pytest gnpy tests -q --ignore=tests/test_invocation.py`）确认新增文件未破坏既有测试与 doctest。
